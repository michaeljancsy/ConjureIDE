[package]
name = "conjuredsp"
version = "0.1.0"
edition = "2024"
description = "DSP building blocks for ConjureDSP Rust/WASM presets"

[lib]
crate-type = ["rlib"]

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
