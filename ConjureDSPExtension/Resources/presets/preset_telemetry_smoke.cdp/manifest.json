{
  "schemaVersion": 2,
  "entry": "process.rs",
  "language": "rust",
  "params": [
    { "name": "drive", "min": 1.0, "max": 10.0, "default": 1.0, "unit": "x" }
  ],
  "ui": {
    "entryHTML": "ui/index.html",
    "width": 420,
    "height": 220,
    "fps": 30,
    "audioFrames": true
  },
  "meta": {
    "description": "Smoke test for the DSP→UI telemetry channel. The script publishes an envelope follower output and a per-block input peak; the UI shows them as bars driven from frame.telemetry."
  }
}
